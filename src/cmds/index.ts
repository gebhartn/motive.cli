import handleGet from './get'
import handleDelete from './del'
import handleAdd from './add'
import handleDefault from './default'
import handleRegister from './register'
import handleLogin from './login'

export const Commands = {
  handleGet,
  handleDelete,
  handleAdd,
  handleDefault,
  handleRegister,
  handleLogin,
}
